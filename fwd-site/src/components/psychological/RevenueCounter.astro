---
import RevenueLossCounter from './RevenueLossCounter.tsx';

interface Props {
  dailyLoss?: number;
  businessType?: string;
}

const props = Astro.props;
---

<RevenueLossCounter client:visible {...props} />